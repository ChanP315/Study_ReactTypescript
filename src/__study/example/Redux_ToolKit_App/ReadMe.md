 + example: Redux_ToolKit_App
     + createSlice({
            name: string
            initialState: {obj},
            reducers: {func}
        })
        export const Actions = counterSlice.actions
        export default counterSlice.reducer